# Copy this file to .env and fill in real values. .env is gitignored.

# OpenAI — used for one of the role models (e.g., judge or simulated user)
OPENAI_API_KEY=

# OpenRouter — gives access to Claude / Gemini / Llama / DeepSeek etc. via one endpoint
OPENROUTER_API_KEY=

# Optional overrides (defaults live in config/*.yaml)
# TARGET_MODEL=
# USER_SIM_MODEL=
# JUDGE_MODEL=
